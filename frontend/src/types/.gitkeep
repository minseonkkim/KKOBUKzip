# 타입을 저장하는 폴더
#
# .d.ts 혹은 .ts 로 생성. 코딩 스타일과 상황에 맞게 type과 interface 혼용 허용
# 도메인 명으로 생성할 경우에 동일 데이터를 중복으로 작성해야 할 경우가
# 많을 것으로 생각되어(판매/경매/유저 도메인의 거북이 데이터는 동일)
# Items의 카테고리를 기준으로 분류

# 모든 타입을 types 폴더 안에 선언하지 않아도 되고,
# 두 번 이상 사용하는 타입만 이 곳으로 옮겨도 됨.
# 해당 타입의 스코프를 명확하게 할 수 있고,
# 같은 파일에서 관리함으로 (코드 길이에 따라) 책임 관리와 유지 보수가 용이
#
# 혹은 타입이 복잡하거나 확장 가능성이 있을 경우에도 이곳에 작성
# 이라고 쓰고 그냥 interface로 모든 타입을 다 여기에 넣을 것으로 생각됨...