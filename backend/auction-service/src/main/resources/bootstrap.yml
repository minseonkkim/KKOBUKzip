spring:
  application:
    name: auction-service
  profiles:
    active: prod
  cloud:
    loadbalancer:
      enabled: true
    config:
      uri: http://127.0.0.1:8888  # Config Server URI 설정
      name: auction-service         # 서비스 이름
