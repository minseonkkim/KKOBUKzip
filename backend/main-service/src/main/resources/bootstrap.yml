spring:
  application:
    name: main-service
  profiles:
    active: {SPRING_PROFILES_ACTIVE}
  cloud:
    loadbalancer:
      enabled: true
    config:
      uri: http://127.0.0.1:8888
      name: main-service
